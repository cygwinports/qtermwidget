--- origsrc/qtermwidget-0.8.0/CMakeLists.txt	2017-10-21 14:01:23.000000000 -0500
+++ src/qtermwidget-0.8.0/CMakeLists.txt	2018-02-26 14:08:19.816960700 -0600
@@ -271,6 +271,8 @@ install(TARGETS ${QTERMWIDGET_LIBRARY_NA
     DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     EXPORT "${QTERMWIDGET_LIBRARY_NAME}-targets"
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     PUBLIC_HEADER
     COMPONENT Runtime
 )
